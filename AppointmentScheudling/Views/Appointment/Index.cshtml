<div class="row">
    <div class="container">
        @if (User.IsInRole(AppointmentScheudling.Utility.Helper.Admin))
        {
            <div class="form-group rounded col-2" id="divDoctorList">
                <label> Select Doctor:</label>
                @Html.DropDownList("doctorId", new SelectList(ViewBag.DoctorList,"Id", "Name"), new {@class="form-control select2"})
            </div>
        }
        <div id="calendar"></div> 
    </div>
</div>

<!-- Partial view for Add or Edit any apointment -->
@*<partial name="_AddEditAppointment" />*@
